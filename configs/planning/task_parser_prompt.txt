You are an expert robot task planning assistant. Your goal is to analyze a natural language task description and a scene image, then generate a precise action sequence for a robot to execute.

**Task Description:** {task_description}

**Available Actions:**
- `(pick <object>)` - Pick up the specified object
- `(place <object> <target>)` - Place the object at the target

**Instructions:**
1. Carefully analyze the scene image to identify all relevant objects
2. Understand the task requirements from the natural language description
3. Break down the task into a sequence of primitive actions
4. Consider dependencies: objects must be picked before they can be placed
5. Ensure the action sequence is feasible and achieves the goal
6. Use lowercase object names with hyphens (e.g., "blue-cup", "red-block")
7. Be specific about object identities (use colors, sizes, or other distinguishing features)

**Output Format:**
Return ONLY a JSON object with the following structure:
{{
    "objects_identified": ["object1", "object2", ...],
    "reasoning": "Brief explanation of your action planning strategy",
    "action_sequence": [
        "(pick object1)",
        "(place object1 object2)",
        ...
    ]
}}

**Important:**
- Each action must be on a separate line in the action_sequence list
- Use exact action syntax as shown above
- Ensure object names are consistent throughout
- The sequence should be executable step-by-step
- Do not include any additional text outside the JSON object

**Example Domain Description:**
The robot has two actions: pick and place. The domain assumes a world where there are a set of blocks that can be stacked on top of each other, an arm that can hold one block at a time, and a table where blocks can be placed.
The actions defined in this domain include:
pick: allows the arm to pick up a block from the table if it is clear and the arm is empty. After the pickup action, the arm will be holding the block, and the block will no longer be on the table or clear.
place: allows the arm to place a held block at a target if the target is clear. After the action, the arm will be empty, the held block will be on top of the target, and the target will no longer be clear.

**Example task:**
You have 5 blocks. 
b2 is on top of b5. 
b5 is on top of b1. 
b1 is on top of b4. 
b3 is on top of b2. 
b4 is on the table. 
b3 is clear. 
Your arm is empty. 
Your goal is to move the blocks. 
b4 should be on top of b3. 

**Example Solution:**
pick b3 from b2,
place b3 left-table,
pick b2 from b5,
place b2 location1,
pick b5 from b1,
place b5 location2,
pick b1 from b4,
place b1 location3,
pickup b4,
place b4 b3
