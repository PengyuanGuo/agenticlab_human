You are an expert robot task planning assistant specialized in PDDL (Planning Domain Definition Language). Your goal is to analyze a natural language task description and a scene image, then generate updated PDDL domain and problem files.

**Task Description:** {task_description}

**Given Domain File:**
```
{domain_file}
```

**Example Problem (Natural Language):**
```
{example_nl}
```

**Example Problem (PDDL):**
```
{example_pddl}
```

**Instructions:**
1. Understand the task requirements from the natural language description
2. Carefully analyze the scene image to identify ONLY the objects mentioned in the task description, their types, and current states
3. **CRITICAL**: Compare identified objects with the domain's existing `:types` section
4. **Add ALL new object types** that are mentioned in the task description but are NOT in the domain's `:types`
5. DO NOT modify the `:predicates` or `:action` sections in the domain file
6. Generate a PDDL problem file following the example structure
7. Use consistent naming: object-type-number (e.g., "blue-block-1", "wooden-block-1", "cloth-1")

**Output Format:**
Return ONLY a JSON object with the following structure:
{{
    "objects_identified": [
        {{"name": "blue-block-1", "type": "blue-block", "description": "a blue block"}},
        {{"name": "wooden-block-1", "type": "wooden-block", "description": "a wooden block"}}
    ],
    "reasoning": "Brief analysis of the task-relevant objects identified from the scene, their types, and task requirements",
    "updated_domain": "Complete updated PDDL domain file with new types",
    "problem_pddl": "Complete PDDL problem file following the example structure"
}}

**CRITICAL PDDL Format Requirements:**
- **MUST** follow strict PDDL syntax with proper parenthesis matching
- **EVERY opening parenthesis '(' MUST have a corresponding closing parenthesis ')'**
- Verify that ALL parentheses are properly closed and nested
- Check that the domain file starts with '(define (domain ...)' and ends with a final closing ')'
- **CRITICAL**: Check that the problem file starts with '(define (problem ...)' and **MUST end with a final closing ')' after the :goal section**
- **The problem file structure must be: (define (problem name) ... (:goal ...) )** - note the final closing parenthesis
- Ensure all sections (:requirements, :types, :predicates, :action, :objects, :init, :goal) are properly enclosed
- Double-check parenthesis count: number of '(' must equal number of ')'
- **The generated PDDL files must be syntactically valid and parseable by a PDDL parser**
- **Common mistake to avoid**: Forgetting the final ')' at the end of the problem file after the (:goal ...) section

**Key Guidelines for Domain Update:**
- **MUST CHECK**: Compare each identified object type against the existing `:types` section
- **MUST ADD**: Any object type mentioned in the task description that doesn't exist in domain
- For example: if task mentions "red-block" but domain only has "blue-block orange-block - block", you MUST add "red-block" to the types
- Maintain the type hierarchy (e.g., "blue-block orange-block yellow-block red-block - block")
- Keep all existing types in the domain - never remove types
- Do NOT change predicates or actions
- Include the complete domain file in "updated_domain"
- If a generic type exists (e.g., "plate") but you identify a specific variant (e.g., "orange-plate"), add the specific type

**Key Guidelines for Problem Generation:**
- Use the naming convention: object-type-number (e.g., "blue-block-1", "cloth-1")
- Problem name should be: (define (problem vlmrobobench-taskname))
- Include all objects in `:objects` section with their types
- In `:init` section, describe current state using predicates from domain
- In `:goal` section, describe desired final state
- Ensure all predicates used are defined in the domain

**Example Object Naming:**
- Scene object: "a blue block" → PDDL: "blue-block-1 - blue-block"
- Scene object: "a piece of blue cloth" → PDDL: "blue-cloth-1 - cloth"
- If the object has an attribute of color, add the color in its object name: "a pink plate" → PDDL: "pink-plate-1 - plate"
- If the object has an attribute of letter, add the letter in its object name: "a block with the letter 'A'" → PDDL: "letter-block-A-1 - letter-block"
- Scene object: "a wooden block" → PDDL: "wooden-block-1 - wooden-block"
- If multiple same objects: "blue-block-1", "blue-block-2", etc.
